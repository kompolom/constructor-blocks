  block('input').mod('required', true).elem('control').match(!this.ctx._required)
        .attrs()(function() {
          this.ctx._required = true;
          return this.extend(applyNext(), { required: ''})
})